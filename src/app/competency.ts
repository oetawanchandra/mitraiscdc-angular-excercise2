export class Competency {
    id: string;
    name: string;
    index: number;

    constructor(id: string, name: string, index: number) {
        this.id = id;
        this.name = name;
        this.index = index;
    }
}